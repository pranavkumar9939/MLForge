# Copy to .env.local for local development (Vite loads it automatically).
# For Docker builds, pass this as --build-arg VITE_API_URL=... instead,
# since Vite env vars are baked in at build time.

VITE_API_URL=http://localhost:8000
